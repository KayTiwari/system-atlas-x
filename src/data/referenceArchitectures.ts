import type { ReferenceArchitecture } from "@/lib/learnTypes";

/** Senior-level reference answers users compare their design against. */
export const REFERENCE_ARCHITECTURES: ReferenceArchitecture[] = [
  {
    id: "ref-url-shortener",
    scenarioId: "url-shortener",
    title: "URL Shortener - Senior Reference",
    componentIds: [
      "web_app",
      "api_gateway",
      "rate_limiter",
      "api_service",
      "cache",
      "sql_database",
      "analytics_pipeline",
      "monitoring",
    ],
    explanation:
      "This is a read-heavy system, so the whole design optimizes the redirect path. A client hits the API through a gateway with a rate limiter protecting the public create endpoint. The application service generates a short code (base62 of a counter or a hashed id) and stores the mapping in the database, the source of truth. Because the same hot links are requested constantly, redirects read from a cache first and fall back to the database on a miss. Click tracking is emitted as an async event into an analytics pipeline so it never slows down a redirect.",
    keyFlows: [
      "Create: client → gateway (rate limited) → service → write mapping to DB → return short code",
      "Redirect: client → service → cache hit returns URL; on miss read DB, populate cache, 301/302",
      "Analytics: redirect emits a click event → analytics pipeline (async), never on the hot path",
    ],
    tradeoffs: [
      "Cache-aside gives big latency wins but needs TTLs and invalidation",
      "Counter-based codes are dense but reveal volume; hashing hides it but can collide",
      "301 (permanent) is cacheable by browsers but makes later edits hard; 302 keeps control",
    ],
    interviewerFollowUps: [
      "How do you generate unique short codes at scale?",
      "What's your cache hit ratio target and eviction policy?",
      "How do you stop abuse of the create endpoint?",
      "How would you support custom aliases and expiry?",
    ],
  },
  {
    id: "ref-realtime-chat",
    scenarioId: "realtime-chat",
    title: "Real-Time Chat - Senior Reference",
    componentIds: [
      "web_app",
      "load_balancer",
      "auth_provider",
      "api_service",
      "event_bus",
      "cache",
      "sql_database",
      "queue",
      "notification_provider",
      "monitoring",
    ],
    explanation:
      "Clients hold WebSocket connections to stateless gateway instances behind a load balancer. Connection state lives in a shared cache (which instance holds which user) rather than in any single server, so the fleet scales out. When a message is sent it is persisted to the database for durability and history, then published to an event bus that fans it out to the recipients' connections. If a recipient is offline, a job is queued to send a push notification through a provider. Messages are ordered per conversation and delivered at-least-once, so clients dedupe by message id.",
    keyFlows: [
      "Connect: client → LB → stateless socket server → register presence in shared cache",
      "Send: client → service → persist message → publish to event bus → fan out to online recipients",
      "Offline: event bus → queue → worker → push notification provider",
    ],
    tradeoffs: [
      "WebSockets give low latency but make connection state and scaling harder than HTTP",
      "At-least-once delivery is simpler than exactly-once but requires client-side dedupe",
      "Storing every message is durable but grows fast; needs archival/retention",
    ],
    interviewerFollowUps: [
      "How do you scale millions of concurrent WebSocket connections?",
      "How do you guarantee message ordering within a conversation?",
      "What happens to messages sent while a user is offline?",
      "How do presence and read receipts work without hammering the DB?",
    ],
  },
  {
    id: "ref-video-streaming",
    scenarioId: "video-streaming",
    title: "Video Streaming - Senior Reference",
    componentIds: [
      "web_app",
      "load_balancer",
      "api_service",
      "object_storage",
      "queue",
      "worker",
      "dead_letter_queue",
      "cdn",
      "cache",
      "search",
      "recommendation_service",
      "analytics_pipeline",
      "monitoring",
    ],
    explanation:
      "Uploads land in object storage and a transcode job is queued. Workers transcode to multiple resolutions for adaptive bitrate streaming, writing renditions back to object storage; failed jobs route to a dead-letter queue for triage. Playback is served from a CDN sitting in front of object storage, so the global read volume never touches the origin. Metadata, search, and recommendations are separate services reading from the database and an analytics pipeline that tracks views and watch time asynchronously.",
    keyFlows: [
      "Upload: client → service → object storage → enqueue transcode job",
      "Transcode: worker pulls job → produces renditions → writes back to storage (failures → DLQ)",
      "Playback: client → CDN edge → object storage origin on cache miss",
      "Discovery: search + recommendation service; views flow to the analytics pipeline async",
    ],
    tradeoffs: [
      "CDN delivery is essential for global scale but adds invalidation and cost concerns",
      "Adaptive bitrate improves UX but multiplies storage and transcode cost",
      "A separate recommendation service adds power but needs feature data and retraining",
    ],
    interviewerFollowUps: [
      "How do you transcode at scale without blocking uploads?",
      "How does adaptive bitrate streaming choose a rendition?",
      "How do you keep playback latency low globally?",
      "How do you handle a transcode job that keeps failing?",
    ],
  },
  {
    id: "ref-ride-sharing",
    scenarioId: "ride-sharing",
    title: "Ride Sharing / Dispatch - Senior Reference",
    componentIds: [
      "mobile_app",
      "load_balancer",
      "api_gateway",
      "api_service",
      "event_bus",
      "cache",
      "sql_database",
      "queue",
      "idempotency_layer",
      "payment_provider",
      "notification_provider",
      "monitoring",
    ],
    explanation:
      "Drivers stream location updates that are written to a geospatial index in a low-latency store (e.g. Redis with geohashing). When a rider requests a trip, the matching service queries the index for nearby available drivers rather than scanning everyone, then dispatches an offer over the event bus. The trip moves through a state machine persisted in the database. At trip end, payment goes through the provider with an idempotency key so a retry never double-charges, and the rider is notified asynchronously.",
    keyFlows: [
      "Location: driver → service → geospatial index in cache (frequent updates)",
      "Match: rider request → query nearby drivers → dispatch offer via event bus",
      "Trip: state machine in DB (requested → matched → ongoing → completed)",
      "Payment: trip end → payment provider with idempotency key → notify rider",
    ],
    tradeoffs: [
      "A geospatial cache index is fast but is not the durable source of truth",
      "Event-driven dispatch decouples matching but is harder to trace",
      "Idempotent payment adds a key store but is non-negotiable for correctness",
    ],
    interviewerFollowUps: [
      "How do you find nearby drivers without scanning the whole fleet?",
      "How do you prevent double-charging at trip completion?",
      "How do you handle a driver disconnecting mid-trip?",
      "How would you add surge pricing?",
    ],
  },
  {
    id: "ref-payment-processing",
    scenarioId: "payment-processing",
    title: "Payment Processing - Senior Reference",
    componentIds: [
      "web_app",
      "api_gateway",
      "auth_provider",
      "api_service",
      "idempotency_layer",
      "payment_provider",
      "webhook_handler",
      "sql_database",
      "queue",
      "dead_letter_queue",
      "audit_log",
      "notification_provider",
      "secrets_manager",
      "monitoring",
    ],
    explanation:
      "Payment systems are designed around correctness, idempotency, auditability, and safe retries. The client initiates checkout through the API, the backend creates a payment intent with the provider using an idempotency key, and provider webhooks - not the synchronous response - update the source-of-truth database. Webhooks are signature-verified, stored raw, then processed asynchronously through a queue with a dead-letter queue for failures. Every money movement writes to an append-only audit log, and a reconciliation job compares internal state against the provider.",
    keyFlows: [
      "Checkout: client → backend creates payment intent (idempotency key) → returns client secret",
      "Confirm: provider → webhook handler verifies signature → stores event → queue → process",
      "State: webhook processing updates order/payment in the DB + writes audit entry",
      "Reconcile: scheduled job compares provider state vs internal records",
    ],
    tradeoffs: [
      "Provider-hosted checkout cuts PCI scope but gives less UI control",
      "Async webhooks improve reliability but create eventual consistency",
      "Storing payment state locally improves UX but requires reconciliation",
    ],
    interviewerFollowUps: [
      "How do you prevent duplicate charges?",
      "What happens if the webhook arrives before the API response?",
      "What if the payment provider is down?",
      "How do you reconcile internal payment status with the provider?",
    ],
  },
  {
    id: "ref-notification-service",
    scenarioId: "notification-service",
    title: "Notification Service - Senior Reference",
    componentIds: [
      "api_service",
      "event_bus",
      "queue",
      "worker",
      "dead_letter_queue",
      "idempotency_layer",
      "notification_provider",
      "sql_database",
      "scheduler",
      "monitoring",
    ],
    explanation:
      "Other services emit events (or call the API) to request a notification. Requests are enqueued so a provider outage becomes a retry, not a dropped message. Workers pull jobs, check user preferences and suppression lists, then send through the provider with retries; persistently failing jobs route to a dead-letter queue. An idempotency key per logical event prevents duplicate sends from at-least-once delivery. Delivery status is tracked in the database, and a scheduler drives digests and quiet-hours batching.",
    keyFlows: [
      "Request: service → API/event bus → enqueue notification job",
      "Send: worker → check prefs/suppression → provider (retries) → record status",
      "Failure: max retries → DLQ + alert; idempotency key dedupes resends",
      "Batch: scheduler triggers digests and respects quiet hours",
    ],
    tradeoffs: [
      "Queueing adds latency but is what makes delivery resilient",
      "At-least-once + idempotency is simpler than exactly-once and safe enough",
      "Provider failover adds reliability but also routing complexity and cost",
    ],
    interviewerFollowUps: [
      "How do you guarantee a user isn't notified twice for one event?",
      "What happens when the email/SMS provider is down?",
      "How do you respect opt-outs and quiet hours?",
      "How would you add provider failover or least-cost routing?",
    ],
  },
  {
    id: "ref-file-upload",
    scenarioId: "file-upload",
    title: "File Upload & Processing - Senior Reference",
    componentIds: [
      "web_app",
      "auth_provider",
      "api_service",
      "object_storage",
      "malware_scanner",
      "queue",
      "worker",
      "dead_letter_queue",
      "cdn",
      "monitoring",
    ],
    explanation:
      "The client requests a signed URL and uploads directly to a quarantine bucket in object storage, keeping large bytes off the application servers. The upload enqueues a processing job; a worker scans the file for malware and only promotes it to the served bucket if it passes - failures route to a DLQ. Derived outputs (thumbnails, parsed text) are written back to storage. Access is controlled with signed URLs and metadata/ACLs in the database, and clean public assets are served through a CDN.",
    keyFlows: [
      "Upload: client → signed URL → direct PUT to quarantine bucket → enqueue job",
      "Process: worker → malware scan → on pass promote + generate derivatives (fail → DLQ)",
      "Serve: signed URL / CDN for clean assets; metadata + ACL checked in the DB",
    ],
    tradeoffs: [
      "Direct-to-storage uploads scale but need careful signed-URL scoping",
      "Async processing keeps requests fast but adds a status model",
      "Scanning adds latency before a file is available but prevents serving malware",
    ],
    interviewerFollowUps: [
      "Where do files live and how do you control access?",
      "How do you keep large uploads off the request path?",
      "How do you stop the system from serving malware?",
      "How do you show processing status to the user?",
    ],
  },
  {
    id: "ref-rate-limiter",
    scenarioId: "rate-limiter",
    title: "Distributed Rate Limiter - Senior Reference",
    componentIds: [
      "api_gateway",
      "rate_limiter",
      "cache",
      "config_service",
      "api_service",
      "monitoring",
    ],
    explanation:
      "The limiter runs at the gateway so abusive traffic is rejected before it reaches services. Counters live in a shared low-latency store (Redis) so enforcement is consistent across every instance - per-instance counters would let clients exceed limits by spreading requests. A token-bucket or sliding-window algorithm balances burst tolerance against strictness, and limits per tier/endpoint come from a config service so they change without a redeploy. On a store outage the system fails open or closed by deliberate choice, and 429s carry Retry-After.",
    keyFlows: [
      "Request: client → gateway → rate limiter checks shared counter in cache",
      "Allow/deny: under limit → forward; over limit → 429 with Retry-After",
      "Config: per-tier/per-endpoint limits read from the config service",
    ],
    tradeoffs: [
      "Sliding-window log is accurate but heavier; counter is cheap but coarser",
      "Shared store is consistent but adds a hop; local cache + async sync trades accuracy for speed",
      "Fail-open favors availability; fail-closed favors protection - pick per endpoint",
    ],
    interviewerFollowUps: [
      "Why not count per-instance?",
      "Token bucket vs sliding window - when would you pick each?",
      "What happens if the shared store is down?",
      "How do you support different limits per customer tier?",
    ],
  },
  {
    id: "ref-ecommerce-checkout",
    scenarioId: "ecommerce-checkout",
    title: "E-commerce Checkout - Senior Reference",
    componentIds: [
      "web_app",
      "api_gateway",
      "api_service",
      "cache",
      "sql_database",
      "idempotency_layer",
      "payment_provider",
      "webhook_handler",
      "queue",
      "dead_letter_queue",
      "notification_provider",
      "audit_log",
      "monitoring",
    ],
    explanation:
      "Checkout reserves inventory in the database inside a transaction (or with an atomic decrement) so concurrent buyers can't oversell. Payment goes through the provider with an idempotency key, so a retried checkout never double-charges or creates a duplicate order. The order moves through a state machine; once the payment webhook confirms, fulfillment is handed off asynchronously through a queue so a slow warehouse system never blocks checkout. Confirmations are sent via a notification provider and money movements are audited.",
    keyFlows: [
      "Checkout: cart → reserve inventory (transaction) → create order (pending)",
      "Pay: payment provider with idempotency key → webhook confirms → order paid",
      "Fulfill: enqueue fulfillment job (failures → DLQ) → notify customer",
    ],
    tradeoffs: [
      "Reserving inventory up front prevents oversell but can hold stock on abandoned carts",
      "Async fulfillment decouples checkout but adds eventual consistency",
      "Idempotency keys add a store but are required to avoid duplicate orders/charges",
    ],
    interviewerFollowUps: [
      "How do you prevent overselling under concurrent checkouts?",
      "How do you avoid duplicate charges and orders on retry?",
      "What happens if payment succeeds but order creation fails?",
      "How do you survive a flash-sale traffic spike?",
    ],
  },
  {
    id: "ref-form-analytics",
    scenarioId: "form-analytics",
    title: "Form Analytics / Intake Dashboard - Senior Reference",
    componentIds: [
      "web_app",
      "api_gateway",
      "rate_limiter",
      "api_service",
      "sql_database",
      "object_storage",
      "queue",
      "analytics_pipeline",
      "data_warehouse",
      "admin_dashboard",
      "auth_provider",
      "audit_log",
      "notification_provider",
      "monitoring",
    ],
    explanation:
      "Embeddable forms post submissions to a public endpoint protected by a gateway and rate limiter (the public surface invites spam and abuse). Submissions are written to the database as the source of truth and any attachments go to object storage. Heavy work - enrichment, trend computation - runs asynchronously off a queue and lands in a data warehouse via an analytics pipeline, keeping the write path fast. Staff use an admin dashboard behind its own auth and authorization boundary, and every access to submitter data writes to an audit log for privacy and compliance.",
    keyFlows: [
      "Intake: embeddable form → gateway (rate limited) → service → store submission (+ files in storage)",
      "Process: enqueue enrichment/analytics job → pipeline → data warehouse",
      "Review: staff → admin dashboard (auth + RBAC) → query/act on records → audit log entry",
      "Notify: new high-priority intake → notification provider alerts staff",
    ],
    tradeoffs: [
      "A public intake endpoint maximizes reach but must be rate limited and validated",
      "A separate warehouse enables trend queries but adds load lag from the operational DB",
      "Audit logging adds write volume but is essential for privacy and compliance",
    ],
    interviewerFollowUps: [
      "How do you protect a public intake endpoint from spam and abuse?",
      "How do you keep submitter data private and access-controlled?",
      "How do you compute trends without slowing down intake?",
      "How do you prove who accessed a given submission?",
    ],
  },
];

export function getReferenceArchitecture(
  id: string
): ReferenceArchitecture | undefined {
  return REFERENCE_ARCHITECTURES.find((r) => r.id === id);
}
